---
interface Props {
  id: string;
  data?: unknown;
  json?: string;
}

const { id, data, json } = Astro.props as Props;
const payload = json ?? JSON.stringify(data ?? null);
---
<script is:inline id={id} type="application/json" set:html={payload}></script>
