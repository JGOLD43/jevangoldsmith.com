---
interface Props {
  selector: string;
  intrinsicSize: string;
}

const { selector, intrinsicSize } = Astro.props as Props;
---
<style is:inline set:html={`${selector}{content-visibility:auto;contain-intrinsic-size:${intrinsicSize};}`} />
