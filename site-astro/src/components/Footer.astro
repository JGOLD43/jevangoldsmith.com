---
// Server-renders the legacy footer partial (_src/partials/footer.html) at
// build time, with data-analytics decoration via lib/chrome.
import { renderFooter, currentPageFromUrl } from '../lib/chrome';
const html = renderFooter(currentPageFromUrl(Astro.url.pathname));
---
<Fragment set:html={html} />
