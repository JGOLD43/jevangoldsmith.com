---
import raw from '../../../data/products.json';
import Base from '../layouts/Base.astro';
import { renderResourceCard } from '../lib/free-resources-render';

interface Resource {
  id: string;
  slug?: string;
  title: string;
  category?: string;
  type?: string;
  shortDescription?: string;
  description?: string;
  ctaLabel?: string;
  downloadUrl?: string;
  checkoutUrl?: string;
}

interface ResourceCategory {
  id: string;
  title: string;
  icon?: string;
}

const resources: Resource[] = (raw as { resources?: Resource[] }).resources ?? [];
const resourceCategories: ResourceCategory[] = (raw as { resourceCategories?: ResourceCategory[] }).resourceCategories ?? [];

const cards = resources.map((r) => renderResourceCard(r, resourceCategories)).join('\n            ');
---
<Base
  title="Useful Resources for Thinking, Building &amp; Learning"
  description="Templates, guides, and practical tools from Jevan Goldsmith for reflection, better thinking, reading, and useful work."
  canonical="https://jevangoldsmith.com/free-resources.html"
  pageType="CollectionPage"
>
  <main class="resources-content">
    <section class="resources-intro">
      <p class="resources-eyebrow">Useful Resources</p>
      <h1 class="resources-title">Practical artifacts from the archive.</h1>
      <p class="resources-subtitle">Templates, guides, and tools I use or am shaping into something useful. Start with the weekly review.</p>
      <div class="collection-cta-row">
        <a href="free-resources.html" class="btn-primary" data-analytics="cta" data-cta-id="free-resources" data-cta-location="resources-hero">Useful Resources</a>
        <a href="field-notes.html" class="btn-secondary" data-analytics="cta" data-cta-id="newsletter" data-cta-location="resources-hero">Get Field Notes</a>
        <a href="contact.html" class="btn-secondary" data-analytics="cta" data-cta-id="contact" data-cta-location="resources-hero">Contact Jevan</a>
      </div>
    </section>

    <div class="resources-grid">
      <Fragment set:html={cards} />
    </div>

    <section class="resources-section">
      <p class="section-eyebrow">Philosophy</p>
      <h2 class="section-title">Why Free?</h2>
      <p class="section-text">I create these resources because organizing my own thinking makes the thinking better. If they help others along the way, that is the whole point. No fake scarcity, no hard sell, just useful tools available to anyone who wants them.</p>
    </section>

    <section class="resources-section">
      <p class="section-eyebrow">Suggestions</p>
      <h2 class="section-title">Request a Resource</h2>
      <div class="request-card">
        <div class="request-icon"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg></div>
        <div class="request-content">
          <h4>Have an idea?</h4>
          <p>If there's a template, guide, or tool you'd find useful, let me know. I'm always looking for ways to create practical resources.</p>
          <a href="contact.html" class="request-link" data-analytics="cta" data-cta-id="contact" data-cta-location="resources-request">
            Send a suggestion
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
          </a>
        </div>
      </div>
    </section>
  </main>
</Base>

<script>
  // Phase 5 (slice 13) + Phase 7 (slice 5): shelf + grid-zoom as native modules.
  import '../scripts/grid-zoom.ts';
  import '../scripts/shelf.ts';
</script>
